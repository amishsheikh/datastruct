import java.util.Scanner;

public class C {
    public static void main(String[] args) {
        Scanner in =new Scanner(System.in);
        int l= in.nextInt();
        int x= in.nextInt();
        int count=0;
        for (int i = l; i < x; i++) {

            double n=Math.ceil(Math.log(l));
            int c=l%10;
//            int d=l/(10Math.pow(n));
        }
    }
}
